## Phase 7 Handoff

### Completed
- Verified changelog completeness: both `CHANGELOG.md` and `local-mirror/CHANGELOG.md` updated for all phases (Phase 5, 5.1, 6, 7)
- Verified all prior phases have handoff documents in the required format: `PHASE5_HANDOFF.md`, `PHASE5.1_HANDOFF.md`, `PHASE6_HANDOFF.md` — all present and compliant
- Verified `ROLLBACK_AND_REPAIR.md` still references the correct baseline (`2026-03-14`, commit `f25e38320c2fcfebb1a79c0e3e1dcc2ca037a685`) — still valid as no new baseline was cut in this sprint
- Updated `local-mirror/docs/SCHEMA_OVERVIEW.md` to include all Phase 4.1 and Phase 6 schema files that were missing from the manual module section
- Produced consolidated cPanel/operator rollout packet (below) covering all new schema, env, and verification steps from Phases 4.1 and 6
- Produced final blocker and outside-folder follow-up ledgers (below)
- Confirmed no contract change from any sprint phase exists only in code without handoff documentation

### Files Changed
- `programs/lavprishjemmeside/PHASE7_HANDOFF.md` — this document
- `programs/lavprishjemmeside/local-mirror/docs/SCHEMA_OVERVIEW.md` — Phase 4.1 and Phase 6 schema entries added to manual module section
- `programs/lavprishjemmeside/CHANGELOG.md` — Phase 7 entry added
- `programs/lavprishjemmeside/local-mirror/CHANGELOG.md` — Phase 7 entry added

### Tests And Checks
- All prior phase build results confirmed passing per their individual handoffs: Phase 5, 5.1, 6 each verified `npm run build` in `local-mirror/` with no errors
- Regression check: Phase 7 is documentation-only — no frontend or API code was changed
- All schema SQL files verified present on disk: `schema_stock_reservations.sql`, `schema_order_notes.sql`, `schema_shipping_zones.sql`, `schema_customer_accounts.sql`, `schema_email_templates.sql`, `schema_product_reviews.sql`, `schema_abandoned_carts.sql`, `schema_stock_notifications.sql`, `schema_subscriptions.sql`, `schema_email_client.sql`

### Handoff Artifacts
- `programs/lavprishjemmeside/PHASE7_HANDOFF.md` — consolidated operator packet, blocker ledger, outside-folder follow-up ledger
- `programs/lavprishjemmeside/local-mirror/docs/SCHEMA_OVERVIEW.md` — updated manual module section covering Phase 4.1 and Phase 6 schema files
- All per-phase handoff docs are in place: `PHASE5_HANDOFF.md`, `PHASE5.1_HANDOFF.md`, `PHASE6_HANDOFF.md`

### cPanel / Operator Handoff

See the consolidated operator packet below.

### Blockers
- Email IMAP/SMTP proxy routes (`/email/config`, `/email/folders`, `/email/messages`, `/email/send`, `/email/drafts`) are not yet wired — the email client admin page renders the not-configured gate until these are implemented outside this folder (see outside-folder follow-ups)
- Live billing integration for subscription management is not wired — upgrade requests are logged to DB but no billing provider is connected (outside-folder)
- Provider switching is UI-complete and generates the operator packet, but the actual API middleware change that reads `AI_PROVIDER` is an outside-folder server-side step

### Outside-Folder Follow-Ups
- **Email IMAP/SMTP proxy**: `server/src/routes/` must add the six `/email/*` routes defined in `PHASE6_HANDOFF.md`. Required for the email client admin page to leave not-configured state. Owner: internal operator / Agent Enterprise.
- **Subscription usage snapshot cron**: A cPanel cron job must be configured to periodically write to `subscription_usage_snapshots`. Suggested interval: daily. Owner: internal operator.
- **Live billing integration**: A billing provider (Stripe or equivalent) must be integrated to activate subscription payments. The `subscriptions` table schema is ready. Owner: internal operator.
- **Provider routing middleware**: After running the operator packet generated by the Provider tab, the Node app must be restarted via cPanel > Setup Node.js App > Restart. No code change required in-folder; the env change takes effect on restart.
- **Release-health verification**: `npm run lavpris:release-health` must be run from repo root after rollout to confirm the live parent and pilot-client health endpoints report the correct build. Owner: internal operator.

### Changelog
- root changelog: updated — Phase 7 entry added
- local-mirror changelog: updated — Phase 7 entry added

---

## Consolidated cPanel / Operator Rollout Packet

This section consolidates all operator-required actions from Phases 4.1 and 6. Execute each packet in order after pulling the V2 branch to the cPanel repo.

---

### Pre-Rollout Checklist (operator must complete before any SQL)

Per `ROLLBACK_AND_REPAIR.md`:

1. Archive the current cPanel repo for `lavprishjemmeside.dk`
2. Archive the current cPanel repo for `ljdesignstudio.dk`
3. Dump the parent site database (labeled with site, date, time)
4. Dump the pilot-client database (labeled with site, date, time)
5. Preserve current `api/.env` files outside the repo (never commit secrets)
6. Record current `/health` responses immediately before rollout

---

### SQL Packet A — Phase 4.1 Shop Extensions (all shop-enabled sites)

**Purpose:** Add inventory reservation, refund/return, order notes, shipping zones, customer accounts, email template customization, product reviews, back-in-stock notifications, and abandoned cart recovery.

**Affected sites:** Any site with the shop module enabled (`lavprishjemmeside.dk`, `ljdesignstudio.dk` if shop is active).

**Run via:** `node api/run-schema.cjs` covers the core schema. The Phase 4.1 files below are not yet in the default `SCHEMA_ORDER`; apply them individually via cPanel phpMyAdmin or SSH:

**Run order (apply in this sequence):**

| Step | File | Purpose |
|------|------|---------|
| 1 | `api/src/schema_stock_reservations.sql` | Inventory reservation at checkout |
| 2 | `api/src/schema_order_notes.sql` | Customer and admin order notes; `customer_note` column on `orders` |
| 3 | `api/src/schema_shipping_zones.sql` | Country-based shipping zone filtering |
| 4 | `api/src/schema_customer_accounts.sql` | Customer accounts (registration, login, order history) |
| 5 | `api/src/schema_email_templates.sql` | Email template customization table + default seeds |
| 6 | `api/src/schema_product_reviews.sql` | Product reviews with admin moderation |
| 7 | `api/src/schema_stock_notifications.sql` | Back-in-stock opt-in notifications |
| 8 | `api/src/schema_abandoned_carts.sql` | Abandoned cart session capture |

**Idempotency:** All files use `IF NOT EXISTS` / `IF NOT EXISTS` column guards. Safe to re-run.

**Verification queries (run after each file):**
```sql
-- After schema_stock_reservations.sql
SHOW TABLES LIKE 'stock_reservations';

-- After schema_order_notes.sql
SHOW COLUMNS FROM orders LIKE 'customer_note';
SHOW TABLES LIKE 'order_notes';

-- After schema_shipping_zones.sql
SHOW COLUMNS FROM shipping_methods LIKE 'countries';

-- After schema_customer_accounts.sql
SHOW TABLES LIKE 'customer_accounts';

-- After schema_email_templates.sql
SHOW TABLES LIKE 'email_templates';
SELECT COUNT(*) FROM email_templates;
-- Expected: > 0 (default templates seeded)

-- After schema_product_reviews.sql
SHOW TABLES LIKE 'product_reviews';

-- After schema_stock_notifications.sql
SHOW TABLES LIKE 'stock_notifications';

-- After schema_abandoned_carts.sql
SHOW TABLES LIKE 'abandoned_carts';
```

**Rollback (if needed):**
```sql
DROP TABLE IF EXISTS abandoned_carts;
DROP TABLE IF EXISTS stock_notifications;
DROP TABLE IF EXISTS product_reviews;
DROP TABLE IF EXISTS email_templates;
DROP TABLE IF EXISTS customer_accounts;
-- Note: shipping_methods.countries column cannot be removed without ALTER TABLE DROP COLUMN
DROP TABLE IF EXISTS order_notes;
DROP TABLE IF EXISTS stock_reservations;
-- Removing customer_note column from orders:
-- ALTER TABLE orders DROP COLUMN customer_note;
```

**Post-run:** Restart Node app via cPanel > Setup Node.js App > Restart.

---

### SQL Packet B — Phase 6 Master Schema (master DB only)

**Purpose:** Create subscription management, provider config/audit tables.

**Affected sites:** Master DB (`theartis_lavpris` or equivalent) only.

**Artifact:** `api/src/schema_subscriptions.sql`

**Run order:**
1. Run `api/src/schema_subscriptions.sql` on the master database via cPanel phpMyAdmin

**Idempotency:** All `CREATE TABLE IF NOT EXISTS`. Safe to re-run.

**Verification queries:**
```sql
SHOW TABLES LIKE 'subscriptions';
SHOW TABLES LIKE 'provider_config';
SHOW TABLES LIKE 'provider_audit_log';
SELECT * FROM provider_config;
-- Expected: 1 row, active_provider = 'anthropic'
```

**Rollback:** `DROP TABLE IF EXISTS subscriptions, subscription_usage_snapshots, subscription_upgrade_requests, provider_config, provider_audit_log;`

---

### SQL Packet C — Phase 6 Email Client Schema (per client DB)

**Purpose:** Create email infrastructure tables.

**Affected sites:** Each client DB where email client is to be activated (run on each individually).

**Artifact:** `api/src/schema_email_client.sql`

**Run order:**
1. Run on each client DB via cPanel phpMyAdmin

**Idempotency:** All `CREATE TABLE IF NOT EXISTS`. Safe to re-run.

**Verification queries:**
```sql
SHOW TABLES LIKE 'email_accounts';
SHOW TABLES LIKE 'email_messages';
DESCRIBE email_accounts;
```

**Rollback:** `DROP TABLE IF EXISTS email_drafts, email_messages, email_folders, email_accounts;`

---

### Env Packet D — Phase 6 Email Client Activation (per client)

**Purpose:** Activate the email client admin page for a client site.

**Affected sites:** Each client where email is to be enabled.

**Variables to add to `api/.env`:**
```
EMAIL_IMAP_HOST=mail.clientdomain.dk
EMAIL_IMAP_PORT=993
EMAIL_IMAP_SSL=true
EMAIL_SMTP_HOST=mail.clientdomain.dk
EMAIL_SMTP_PORT=587
EMAIL_SMTP_SSL=true
EMAIL_ENCRYPTION_KEY=<32-byte hex — generate: openssl rand -hex 32>
```

**Apply location:** `api/.env` on each client cPanel Node.js app.

**Restart / build requirement:** Restart Node app via cPanel > Setup Node.js App > Restart.

**Verification:**
1. `GET https://api.clientdomain.dk/email/config` → `{ "configured": true }`
2. Navigate to `/admin/email/` — should show the inbox UI (not the not-configured gate)

**Rollback:** Remove the `EMAIL_*` vars from `.env` and restart.

---

### Env Packet E — Phase 6 Provider Switching (master site)

**Purpose:** Apply a provider switch after the Master Hub generates the packet.

**Triggered by:** Master user saving a provider choice in the Provider tab — UI generates this packet automatically. Copy it from the "Kopier" button.

**Variables to update in `api/.env` on the master site:**
```
AI_PROVIDER=<anthropic|openai>
ANTHROPIC_API_KEY=<keep if anthropic>
OPENAI_API_KEY=<required if openai — obtain from platform.openai.com>
```

**Restart / build requirement:** Restart Node app via cPanel > Setup Node.js App > Restart.

**Verification:** `POST /ai/generate` with a test prompt — confirm `model` field in response matches the selected provider.

**Rollback:** Revert `AI_PROVIDER` and restart.

---

### Live Verification Checklist

**Affected sites:** `lavprishjemmeside.dk`, `ljdesignstudio.dk`

**Health endpoints:**
- `https://api.lavprishjemmeside.dk/health` — expect `status: ok`, `version` present
- `https://api.ljdesignstudio.dk/health` — expect `status: ok`, `version` present

**Admin routes to verify:**
- `/admin/` — loads dashboard with stat cards and quick actions
- `/admin/pages` — loads page builder with status dots and sidebar badge
- `/admin/components` — loads with filter/sort controls, status badges, usage counts
- `/admin/styling` — loads with autosave indicator
- `/admin/themes` — loads with visual theme preview cards
- `/admin/master` — loads with 5 tabs (Sites, AI Usage, Claude Code, Provider, Abonnementer)
- `/admin/email` — loads not-configured gate (or inbox if email env is configured)

**Shop routes to verify (shop-enabled sites):**
- `/shop/` — loads shop hero and product grid
- `/shop/produkt/[slug]` — loads product detail with available stock display
- `/admin/shop/dashboard` — loads KPI cards and recent orders
- `/admin/shop/orders` — loads with refund modal accessible

**Critical flows to spot-check:**
1. Admin login and dashboard load
2. Create and publish a page via AI Assembler
3. Shop browse → product → add to cart → checkout → order confirmation
4. Shop admin: create product, view order, trigger refund action
5. Master Hub: AI Usage tab loads fleet summary; Provider tab loads current provider

**Expected success conditions:**
- All admin pages load without JavaScript errors in the browser console
- Shop checkout completes and redirects to order confirmation
- Master Hub Provider tab reads `active_provider` from DB without 500 errors
- No `alert()` calls fire anywhere in the admin — toast notifications only

---

### Rollback Note

**Target baseline:** `2026-03-14` snapshot documented in `ROLLBACK_AND_REPAIR.md`

**Rollback trigger:** Any critical flow remains broken after one bounded repair cycle (see `ROLLBACK_AND_REPAIR.md` for trigger rules).

**Artifacts the operator should restore:**
1. Archived cPanel repo for the affected site (taken in pre-rollout checklist)
2. Database dump for the affected site (taken in pre-rollout checklist)
3. Known-good `api/.env` (preserved in pre-rollout checklist)
4. Rebuild and restart via the SSH-first runbook in `local-mirror/docs/SSH_FIRST_OPERATIONS.md`

---

## Final Blocker Ledger

| Blocker | Phase | Reason | Required Action |
|---------|-------|--------|-----------------|
| Email IMAP/SMTP proxy routes not wired | 6 | Outside-folder — requires `server/src/routes/` changes | Internal operator adds `/email/*` API routes per `PHASE6_HANDOFF.md` route contract |
| Live billing provider not connected | 6 | Outside-folder — requires external billing integration | Internal operator integrates Stripe or equivalent against the `subscriptions` schema |
| Subscription usage snapshot cron not set up | 6 | Outside-folder — requires cPanel cron configuration | Internal operator adds daily cron: `node api/scripts/snapshot-usage.cjs` |
| Provider switching middleware not reading `AI_PROVIDER` | 6 | Outside-folder — `server/src/lib/anthropic-client.js` needs to read the env var | Apply Env Packet E, restart; AI client must read `process.env.AI_PROVIDER` at request time |
| Dashboard date-range filter | 5.1 | Deferred from Phase 5.1 — data model supports it, UI deferred | Phase 7 polishing work (not a blocker for rollout) |
| Components browser full structured props table | 5.1 | Deferred from Phase 5.1 | Phase 7 polishing work (not a blocker for rollout) |
| Active theme CSS vars not auto-injected into AI Assembler system prompt | 5.1 | Partial — prompt settings loaded but theme vars not injected | In-folder implementation: read active theme row from `/theme-settings` and inject into system prompt before generation |

---

## Outside-Folder Follow-Up Ledger

| Follow-Up | Dependency | Interface | Required Owner Action |
|-----------|-----------|-----------|----------------------|
| Email proxy routes | `server/src/routes/` in repo root | Six routes per `PHASE6_HANDOFF.md` contract | Implement `/email/config`, `/email/folders`, `/email/messages`, `/email/messages/:id`, `/email/send`, `/email/drafts` |
| Subscription snapshot cron | cPanel cron scheduler | `POST /master/subscriptions/snapshot` or a Node script | Configure daily cron job via cPanel |
| Live billing integration | External billing provider (Stripe etc.) | POST to billing provider on upgrade request approval | Wire billing API to `subscription_upgrade_requests` table |
| Provider routing in API middleware | `server/src/lib/anthropic-client.js` | `process.env.AI_PROVIDER` env var | Ensure the AI client reads `AI_PROVIDER` at request time (additive env read) |
| Release-health verification | repo-root `package.json` scripts | `npm run lavpris:release-health` | Run from repo root after rollout to confirm live build hashes match |
| cPanel live rollout | SSH-first deploy contract | `local-mirror/docs/SSH_FIRST_OPERATIONS.md` | Pull V2 branch to cPanel repos, run schema packets, restart Node apps, verify health endpoints |
